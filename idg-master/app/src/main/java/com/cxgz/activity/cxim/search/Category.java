package com.cxgz.activity.cxim.search;import org.bytedeco.javacpp.tools.Info;import java.util.ArrayList;import java.util.List;public class Category{    //分类一级名称    private SearchInfo searchTopInfo;    private List<SearchInfo> mCategoryItem = new ArrayList<SearchInfo>();    public Category(SearchInfo searchTopInfo)    {        this.searchTopInfo = searchTopInfo;    }    public SearchInfo getSearchTopInfo()    {        return searchTopInfo;    }    public void addItem(SearchInfo searchInfo)    {        mCategoryItem.add(searchInfo);    }    /**     * 获取Item内容     *     * @param pPosition     * @return     */    public SearchInfo getItem(int pPosition)    {        // Category排在第一位        if (pPosition == 0)        {            return searchTopInfo;        } else        {            return mCategoryItem.get(pPosition - 1);        }    }    /**     * 当前类别Item总数。Category也需要占用一个Item     *     * @return     */    public int getItemCount()    {        return mCategoryItem.size() + 1;    }}